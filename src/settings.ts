/**
 * Kept as AnthemReceiver by the Plus maintainer for existing configuration and
 * cached-accessory migration. Do not rename this with the npm package.
 */
export const PLATFORM_NAME = 'AnthemReceiver';

/**
 * This must match the name of your plugin as defined the package.json
 */
export const PLUGIN_NAME = 'homebridge-anthemreceiver-plus';
